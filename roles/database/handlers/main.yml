---
- name: Re-export NFS shares
  ansible.builtin.command:
    cmd: exportfs -ra
  changed_when: false

- name: Restart autofs
  ansible.builtin.systemd:
    name: autofs
    state: restarted
    enabled: true
