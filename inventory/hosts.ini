# Example inventory. Replace the placeholder hosts below with your own,
# or delete this file entirely if you're using ansible-pull (see
# bootstrap.sh / README.md) -- ansible-pull runs against localhost and
# doesn't need an inventory at all.

[rivendell]
# my-droplet.example.com
# 192.168.1.50   ; ansible_user=root   # UTM VM or physical hardware

[rivendell:vars]
ansible_python_interpreter=/usr/bin/python3
